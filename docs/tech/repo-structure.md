# GroundWork — Repository Structure

**Status:** complete  
**Phase:** 0 P0  
**Author:** Moraen

---

## 1. Top-Level Layout

```
karjat-farms/
├── index.html                  # HTML5 prototype (reference only — not shipped in Unity build)
├── sprites3/                   # Pixel art assets (reference — Phase 1 Sprites import)
├── groundwork-unity/           # Unity 2022.3 LTS project (Phase 1+)
├── docs/                       # All documentation (source of truth)
├── supabase/                   # Migrations, Edge Functions, RLS policies (Phase 2+)
├── scripts/                    # Dev/ops scripts (mandi scrapers, data generators)
├── tests/                      # Unit/integration tests (Python + Unity PlayMode)
├── .gitignore                  # Excludes (see §5)
├── README.md                    # Links to docs/README.md, Phase 0 status
└── LICENSE
```

**Rule:** Unity build artifacts (.apk, .aab) are never committed — CI generates and stores them.

---

## 2. Unity Project Layout (`groundwork-unity/`)

```
groundwork-unity/
├── Assets/
│   ├── KarjatData/            # ScriptableObjects + JSON data (CROPS, seasons, prices)
│   ├── Scenes/                # One scene per game phase
│   ├── Prefabs/               # Tile, crop, UI prefabs
│   ├── Scripts/               # Game logic (FarmingEngine, CropManager, Market)
│   ├── UI/                    # Canvas prefabs, HUD elements
│   ├── Audio/                 # SFX + music stubs
│   └── FarmingEngine/         # Purchased asset integration layer
├── ProjectSettings/
├── Packages/
├── .vscode/                   # Unity debugger config (gitignored locally)
└── groundwork-unity.sln       # .NET solution
```

**Farming Engine import:** Asset store package goes into `Assets/FarmingEngine/`. All Karjat-specific overrides live in `Assets/Scripts/` — never modify the `FarmingEngine/` folder directly (preserve upgrade path).

---

## 3. Documentation Layout (`docs/`)

```
docs/
├── README.md                  # Master doc index — checkboxes for Phase 0
├── dev-log.md                 # Agent session log (3 sentences/session)
├── HANDOFF-MORAEN.md          # Phase 0 assignment + acceptance table
├── gdd/
│   ├── GDD.md                 # Game Design Document
│   └── time-system.md         # Time system spec
├── tech/
│   ├── architecture.md        # System diagram + dual-build strategy
│   ├── repo-structure.md      # This file
│   ├── farming-engine-audit.md # Keep/Strip/Replace table
│   └── dev-workflow.md        # Git SDLC, Unity MCP, machine roles
├── geo/
│   └── karjat-region-bible.md # Karjat T0 region spec (6 crops, water, mandi)
└── karjat-economy/
    ├── crops.json             # Crop data v1 schema
    └── crops.md               # Crop writeup (mandi references)
```

---

## 4. Prototype Assets (`index.html`, `sprites3/`)

- **index.html**: fully playable HTML5 prototype. Kept as design reference. **Never ported into Unity as-is.** Used for:
  - Game feel validation
  - CROPS object extraction (source of truth for Phase 0 data)
  - Loop prototyping before Unity integration
- **sprites3/**: pixel art source files. Unity sprites imported as individual PNGs from this folder at Phase 1 start.

---

## 5. What Never to Commit

```gitignore
# Credentials
.env
*.pem
*.key
supabase/service_role_key.txt

# Unity build artifacts (regenerated by CI)
*.apk
*.aab
*.ipa
builds/

# Unity cache/generated
Library/
Temp/
Logs/
obj/
.vs/
*.sln.docstates

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.router-env/

# Large binaries (use Git LFS)
# *.png (pixel art — commit these)
*.psd
*.ai
```

**Pixel art PNGs are committed** (sprites3/). All other large binaries use Git LFS.

---

## 6. Branch Strategy

```
main                        # Stable — Arsalan merges PRs here
├── docs/phase0-p0          # Phase 0 documentation (Moraen, current)
├── feature/phase1-*        # Phase 1 Unity integration
├── feature/phase2-*        # Phase 2 Supabase backend
└── hotfix/*                # Emergency fixes
```

**Rules:**
- `main` is always deployable
- Feature branches cut from `main`
- PRs require one review before merge
- `docs/phase0-p0` merges to `main` after SA audit (Claude) + trainee review

---

## 7. IDE / Remote Setup

| Machine | Role | Connection |
|---|---|---|
| Mac Mini (Tailscale) | Primary: Unity Editor + Cursor | Tailscale SSH: `ssh appopoleis@100.127.150.60` |
| moraen-Home (Tailscale) | Secondary: docs-only remote SSH | `ssh appopoleis@100.99.243.39` |
| umar-asus (`100.79.34.78`) | Production web + future CI | Not for Unity Editor |

**Cursor Remote SSH for docs (Mac Mini → moraen-Home):**
- Host configured in `.ssh/config` pointing to `100.99.243.39`
- Read-only for docs — edits via PR workflow only

---

## 8. Open Questions (Moraen)

1. **Supabase schema location** (`supabase/`): Phase 2. Is the migration file structure agreed, or should this section be a stub until Phase 2 starts?
2. **Git LFS**: Has it been configured on the repo? If not, need `git lfs install` + LFS track for `.png`, `.psd` files before Phase 1 sprites import.
3. **scripts/** folder: contents not yet defined. Should contain mandi scrapers, crop data generators. Confirm this is the right location vs `tools/` at root.